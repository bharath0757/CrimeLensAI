# Extraction Service
# CrimeLensAI — NLP Entity Extraction & Resolution
#
# Extracts PERSON, PHONE, VEHICLE, UPI_ID, LOCATION, ORG entities
# from FIR text, call records, and transaction logs using spaCy NER
# + regex pipelines. Includes fuzzy matching and entity resolution
# for name variants (e.g., "Rajesh Kumar" ↔ "R. Kumar").
